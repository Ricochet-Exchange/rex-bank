apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: commodo-{{.Values.environment}}
  name: commodo-{{.Values.environment}}
spec:
  type: NodePort
  ports:
  - name: "80"
    port: 80
    targetPort: 80
  selector:
    io.kompose.service: commodo-{{.Values.environment}}
status:
  loadBalancer: {}
